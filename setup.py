from setuptools import setup, find_packages

setup(
    name='IPV8',
    description='The IPV8 library',
    version='0.1',
    url='http://ip-v8.org',
    package_data={'': ['*.*']},
    packages=find_packages()
)
